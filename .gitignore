# Private infra config (real bastions, IPs, project) — NEVER commit.
tunnels.json

# Timestamped backups of any file (may contain the same private infra) — NEVER commit.
*.bak
*.bak.*

# Runtime logs (may contain hostnames, IPs, auth prompts, MOTD banners).
logs/
*.log

# Local watchdog state.
/tmp/fynd-tunnel-*

# Generated launchd plists (if ever copied here).
*.plist

# OS / editor cruft.
.DS_Store
.tool-versions
*.swp
*~
.idea/
.vscode/

# Python cache.
__pycache__/
*.pyc
